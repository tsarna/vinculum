# Brokers the integration suites are run against.
#
# `services:` containers in a GitHub workflow start before checkout and cannot
# mount a file from the repository, which every broker here needs — RabbitMQ a
# definitions file for its users and a certificate for its TLS listener. So the
# brokers are started with compose after checkout instead, one service per job:
#
#   docker compose -f ci/brokers.yml up -d --wait rabbitmq
#
# That is the same command CI runs, so a failure there reproduces locally.
# Fixtures a broker needs built first are made by ci/<service>/setup.sh, and the
# environment its tests read is ci/<service>.env:
#
#   ci/rabbitmq/setup.sh
#   docker compose -f ci/brokers.yml up -d --wait rabbitmq
#   set -a; . ci/rabbitmq.env; set +a
#   go test -race -tags integration ./clients/rabbitmq/...
#
# Ports are published on the loopback interface only, so a broker and its known
# credentials are not reachable from the network — and only one of these can run
# at a time per machine, which is all any one job needs.

# Named, because compose otherwise takes the project name from this file's
# directory, and every repository that follows this layout calls it ci/. Two
# projects with one name are one project: `up` in one repository replaces the
# other's broker with its own, and `down` in one takes the other's broker with it
# or fails on the network they share.
name: vinculum-ci

services:
  rabbitmq:
    image: rabbitmq:4-management
    # Fixed, because the node name is rabbit@$HOSTNAME and a random one makes
    # the log harder to read across runs.
    hostname: vinculum-rabbitmq
    ports:
      - "127.0.0.1:5672:5672" # amqp
      - "127.0.0.1:5671:5671" # amqps
      - "127.0.0.1:15672:15672" # management API
    volumes:
      - ./rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro
      - ./rabbitmq/definitions.json:/etc/rabbitmq/definitions.json:ro
      - ./rabbitmq/certs:/etc/rabbitmq/certs:ro
    # The check ends by looking for the vhost from the definitions file, so that
    # `--wait` means "provisioned" rather than "a port answers". The management
    # API listens before that file is imported — only the AMQP and AMQPS
    # listeners wait for it — so a management call that arrived on a port
    # answering alone could find no vhost and no users.
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "rabbitmq-diagnostics -q check_running && rabbitmq-diagnostics -q check_port_connectivity && rabbitmqctl -q list_vhosts | grep -qx vinculum",
        ]
      interval: 5s
      timeout: 10s
      retries: 30
      start_period: 10s
