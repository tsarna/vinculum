name: GHCR Cleanup

on:
  schedule:
    - cron: "13 7 * * 0"
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Preview deletions without deleting"
        type: boolean
        default: false
      older-than:
        description: "Delete untagged versions older than"
        type: string
        default: "14 days"

permissions:
  contents: read
  packages: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Clean vinculum
        uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          owner: ${{ github.repository_owner }}
          package: vinculum
          delete-untagged: true
          older-than: ${{ inputs.older-than || '14 days' }}
          exclude-tags: latest,dev,latest-minimal,dev-minimal
          delete-partial-images: false
          validate: true
          dry-run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry-run || false }}
